{
  "name": "privacy-lens-insper", "version": "0.2.0", "private": true,
  "description": "Extensão Firefox acadêmica, implementação incremental.",
  "scripts": {"test": "node --test tests/*.test.cjs", "fixture": "node tests/fixture/server.cjs", "check": "node scripts/check.cjs"},
  "engines": {"node": ">=20"}
}
